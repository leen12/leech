fastapi>=0.110
uvicorn[standard]>=0.29
httpx[socks]>=0.27
# The gateway backend (Puter / NVIDIA NIM) needs nothing beyond httpx.
# The items below are ONLY for the disabled legacy use.ai browser path:
# cloakbrowser
# pip install playwright && playwright install chromium
